name: Semantic Pull-Request
description: Ensures Pull-Requests follow Conventional-Commit standards.

# SRC: https://github.com/amannn/action-semantic-pull-request?tab=readme-ov-file#installation
# pull_request_target:
#   types:
#     - opened
#     - reopened
#     - edited
#     # - synchronize (if you use required Actions)

# NOTE: Set in final file
# permissions:
#   pull-requests: read / write
#
# env:
#   GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

runs:
  using: composite
  steps:
    # SRC: https://github.com/amannn/action-semantic-pull-request/releases/tag/v6.1.1
    - uses: amannn/action-semantic-pull-request@48f256284bd46cdaab1048c3721360e808335d50
      # env:
      #   GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
